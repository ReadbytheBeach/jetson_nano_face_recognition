# jetson_nano_face_recognition

# trainSave.py to generate the "train.pkl" for other py script to use
# train.pkl is a pickle format, to save the binary data as pickle.dump()
# From faceRecognize-4.py / faceRecognize-5-liveVideo.py to faceRecoginze-X.py (x >=4), will load the "train.pkl" by pickle.load(f) function.
